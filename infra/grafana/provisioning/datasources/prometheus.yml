apiVersion: 1
datasources:
  - name: Prometheus
    # Pinned so the checked-in dashboards can reference it by uid. Without this
    # Grafana generates a uid at first start and every provisioned panel loads empty.
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
